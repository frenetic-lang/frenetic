(jbuild_version 1)

(rule
 ((targets  (absorption.pyc))
  (deps     (absorption.py))
  (action   (run python3 -m compileall -b ${^}))
 )
)

(install
  ((section lib)
   (files   (absorption.pyc))
   (package probnetkat)
  ))
