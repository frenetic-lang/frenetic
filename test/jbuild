(jbuild_version 1)

(executable
 ((name test)
  (libraries (probnetkat alcotest qcheck))))

(alias
 ((name runtest)
  (deps (test.exe ${ROOT}/python/absorption.pyc))
  (action (run ${<} --color=always))
))
