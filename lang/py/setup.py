from setuptools import setup

setup(name='netkat',
      version='0.0.1',
      description='Python bindings for the NetKAT policy language and HTTP application API.',
      packages=['netkat'],
      install_requires=['tornado']
)
