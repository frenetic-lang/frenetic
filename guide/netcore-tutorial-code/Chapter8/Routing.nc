(* Fill in the routing policy for the topology in Chapter 8 *)
let routing =
  if switch = 1 then
    drop
  else if switch = 2 then
    drop
  else if switch = 3 then
    drop
  else
    drop
    
routing