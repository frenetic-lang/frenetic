\documentclass{article}
\usepackage{savetrees}
\usepackage{pgfplots}
\pgfplotsset{compat=1.13}
\begin{document}

\begin{figure}
\begin{tikzpicture}
  \begin{axis}[
      xlabel={Number of edge nodes},
      ylabel={Time(in ns)},
      legend style = {at={(0.33,.9)}, anchor=north },
      legend entries={Solution Time,Generation Time}]
    \addplot table [x=EdgeNodes, y=Solution(nsecs), col sep=space]
             {coronet-30-ocaml.results};
             \addplot table [x=EdgeNodes, y=Generation(nsecs), col sep=space]
                      {coronet-30-ocaml.results};
  \end{axis}
\end{tikzpicture}
\caption{Endpoint matching in OCaml}
\end{figure}

\begin{figure}
\begin{tikzpicture}
  \begin{axis}[
      xlabel={Number of edge nodes},
      ylabel={Time(in ns)},
      legend style = {at={(0.33,.9)}, anchor=north },
      legend entries={Formulation Time,Solution Time,Generation Time}]
    \addplot table [x=EdgeNodes, y=Formulation(nsecs), col sep=space]
             {coronet-30-lpe.results};
             \addplot table [x=EdgeNodes, y=Solution(nsecs), col sep=space]
                      {coronet-30-lpe.results};
                      \addplot table [x=EdgeNodes, y=Generation(nsecs), col sep=space]
                               {coronet-30-lpe.results};
  \end{axis}
\end{tikzpicture}
\caption{Endpoint matching using an LP Solver}
\end{figure}

\begin{figure}
\begin{tikzpicture}
  \begin{axis}[
      xlabel={Number of edge nodes},
      ylabel={Time(in ns)},
      legend style = {at={(0.33,.9)}, anchor=north },
    legend entries={Formulation Time,Solution Time,Generation Time}]
    \addplot table [x=EdgeNodes, y=Formulation(nsecs), col sep=space]
             {coronet-30-sate.results};
             \addplot table [x=EdgeNodes, y=Solution(nsecs), col sep=space]
                      {coronet-30-sate.results};
                      \addplot table [x=EdgeNodes, y=Generation(nsecs), col sep=space]
                               {coronet-30-sate.results};
  \end{axis}
\end{tikzpicture}
\caption{Endpoint matching using a SAT solver}

\end{figure}

\end{document}
