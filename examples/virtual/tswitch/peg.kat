(*egress of physical topology*)
(switch=1 and port=1) or
(switch=1 and port=2) or
(switch=2 and port=1) or
(switch=2 and port=2) or
(switch=3 and port=1) or
(switch=3 and port=2) or
(switch=4 and port=1) or
(switch=4 and port=2) or
(switch=5 and port=1) or
(switch=5 and port=2) or
(switch=6 and port=1) or
(switch=6 and port=2) or
(switch=7 and port=1) or
(switch=7 and port=2) or
(switch=8 and port=1) or
(switch=8 and port=2)
