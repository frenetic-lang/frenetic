
true : use_menhir
true : use_str
<*.d.byte> : package(cstruct), package(lwt), package(lwt.unix), package(NetCoreLib)
<*.native> : package(cstruct), package(lwt), package(lwt.unix), package(NetCoreLib)
<**/*.ml{,i}> : package(cstruct), \
                package(lwt), \
                syntax(camlp4o), \
                package(lwt.syntax), \
                package(NetCoreLib)

