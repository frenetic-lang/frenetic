(jbuild_version 1)


(library
 ((name        probnetkat)
  (public_name probnetkat)
  (wrapped true)
  (libraries (
    core
    zarith
    findlib
    frenetic
    open
    ocamlgraph
  ))
  (preprocess (pps (ppx_deriving.std
                    ppx_jane
                   )))

))
