(jbuild_version 1)


(library
 ((name        probnetkat)
  (public_name probnetkat)
  (wrapped true)
  (libraries (
    core
    async
    async_unix
    ;; rpc_parallel
    bin_prot
    zarith
    findlib
    frenetic
    open
    ocamlgraph
    printbox
  ))
  (preprocess (pps (ppx_deriving.std
                    ppx_jane
                   )))

))
