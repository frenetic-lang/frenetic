(jbuild_version 1)

(executable
 ((name         main)
  (public_name  probnetkat)
  (libraries (
    core
    ;; owl
    ;; lacaml
    zarith
  ))
  (preprocess (pps (ppx_deriving.std
                    ppx_enumerate
                    ppx_compare
                    ppx_fields_conv
                    ppx_sexp_conv
                    ppx_hash
                   )))
))
