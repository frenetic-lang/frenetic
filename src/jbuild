(jbuild_version 1)


(library
 ((name        probnetkat)
  (public_name probnetkat)
  (wrapped true)
  (libraries (
    core
    ;; owl
    zarith
    findlib
    frenetic
  ))
  (preprocess (pps (ppx_deriving.std
                    ppx_enumerate
                    ppx_compare
                    ppx_fields_conv
                    ppx_sexp_conv
                    ppx_hash
                   )))

))
